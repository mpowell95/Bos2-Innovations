# content.json — field reference

Everything you author goes in this file. The shell, logo, CSS, JS, header, toolbar, footer,
section wrappers, chevrons, `aria-expanded`/`.open` pairing, open/closed defaults, section
numbering and two-column switching are all generated by `build.py`. Don't write them.

Run: `python3 assets/build.py content.json /mnt/user-data/outputs/Name_FAQ_Guide.html`

Unknown keys are rejected. On any failure the build prints the problem and writes nothing.

## Top level

| Key | Required | Notes |
|---|---|---|
| `title` | yes | Document name. Used in `<title>` and the header. |
| `doc_type_dates` | yes | Header right-hand line, e.g. `"Revocable Living Trust · Restated Mar 2023, original Jun 2011"` |
| `preparer` | yes | Name of the person running the skill. The CP address is added for you. |
| `prepared` | yes | e.g. `"September 2026"` |
| `quick_ref` | yes | Rows of the §1 key-facts table. See below. |
| `sections` | yes | Collapsible sections, in order. Numbering starts at §2 automatically. |
| `layout` | no | `"single"` (default) or `"two-column"` (mirror/paired RLTs only). |
| `grantor_labels` | two-column only | `["Robert", "Margaret"]` — grantor 1 is named first in the preamble. |
| `eyebrow` | no | Teal header label. Defaults to `"Trust & Estate FAQ"`. |
| `banners` | no | Banners at the top of `<main>`, before the Quick Reference. |
| `binder` | no | `{"family": "Sample", "href": "Sample_Estate_Plan_Index.html"}` — generates the back-to-index crumb. Relative filename only. |

## Inline markup in authored text

Answers, banner bodies, table cells and stage bodies accept a little HTML. Permitted classes,
and nothing else:

`amt` (dollar amounts, fractions) · `amt-lapse` (a gift that lapses) · `na` (inapplicable) ·
`not-found` (missing provision) · `section-ref` (citation) · `doc-badge` (Box doc id) ·
`qr-people` · `qr-hl`

Any other class fails the build — it would render unstyled. Never use `<img>`, `data:` URIs,
`mask:`, external stylesheets or emoji; all are rejected (the first three are silently blocked
by the hosting platform's CSP, so they fail invisibly in the browser rather than loudly).

## `quick_ref` rows

```json
{"k": "Grantors", "v": ["Robert A. Sample", "Margaret L. Sample"], "cite": "§1.1"}
```

| Field | Meaning |
|---|---|
| `k` | Label |
| `v` | Value. A **list** renders as stacked one-per-line (`qr-people`) — always use a list for two or more people; never join names with `,` or `·`. |
| `cite` | Section reference, rendered as a quiet chip |
| `hl` | `true` highlights the value. Max 3 per guide, or the build fails — past that, highlighting means nothing. Use it for the 1–2 must-catch facts (current amendment date, payout ages). |
| `not_found` | `true` renders the standard NOT FOUND flag instead of a value |
| `numbered` | A ladder (successor trustees). Renders as a full-width numbered chip list. Items are strings, or `{"text": "...", "badge": "Only difference"}` to mark the one differing tier in a mirror pair. |
| `wide` | `true` makes a row span the full width |

Rows are packed two pairs per row automatically. A `numbered` or `wide` row takes its own row.
A trailing lone fact is fine — leave the second pair empty rather than padding it.

## `sections`

In the order given; `build.py` numbers them from §2. Order comes from
`references/section-guide.md` — omit inapplicable sections, leave no gaps.

```json
{
  "cat": "trustee",
  "title": "Trustee Succession",
  "doc_badge": "optional Box doc id",
  "blocks": [ ... ],
  "items": [ {"q": "Is a bond required?", "a": "No — bond is waived <span class=\"section-ref\">§3.6</span>."} ]
}
```

`cat` must be one of `overview` · `trustee` · `lifecycle` · `distribution` · `legal`. It drives
the section colour and the filter buttons; the mapping is in `references/section-guide.md`.

`blocks` render before the questions. An item may also carry its own `blocks`, rendered after
its answer. Every block ends up inside its section, so nothing escapes the category filter.

**Open/closed state is not yours to set.** §2 opens with its first question open; §3–4 open with
questions closed; §5 onward fully closed. This is the single biggest lever on page height.

## Blocks

**Table** — use for distributions, trustees, charities, withdrawal schedules, agent succession.
Never prose for these.
```json
{"type": "table", "caption": "Successor trustees at a glance",
 "headers": ["Priority", "Name", "Condition", "§Ref"],
 "rows": [["1", "Surviving spouse", "If able and willing", "<span class=\"section-ref\">§3.1</span>"]]}
```

**Flowchart** — the trust structure overview. Don't replace it with prose.
```json
{"type": "flowchart", "stages": [
  {"stage": "Now — Both Grantors Living", "active": true, "body": "...", "cite": "§1.2"},
  {"branch": [
    {"stage": "Marital Trust", "body": "..."},
    {"stage": "Family Trust", "variant": "family-trust", "body": "..."}]},
  {"stage": "Children's Shares", "variant": "children", "body": "...", "cite": "§4.2"}]}
```
`active: true` marks the stage in effect right now. `variant` is `family-trust` or `children`
for a tinted branch. Arrows between stages are generated.

Stage titles name the **event** — "First Death", "Family Trust", "Now — Both Grantors Living".
A title like "Stage 1" or "Active Now" fails the build.

**Banner** — `variant` is `warning` (action needed), `flag` (superseded, conflict, critical) or
`info` (clarifying note). Each gets its own icon shape, so severity reads without colour.
```json
{"type": "banner", "variant": "warning", "label": "Funding gap", "body": "..."}
```

**Two-column** (mirror trusts only) — differing provisions:
```json
{"type": "two_col", "g1": {"head": "Robert", "body": "..."},
                    "g2": {"head": "Margaret", "body": "..."}}
```

**Identical** (mirror trusts only) — a provision that matches in both trusts:
```json
{"type": "identical", "body": "Both trusts waive bond for every trustee."}
```

## What the build checks before writing

Logo intact (16 `<path>`, 1 `<use>`, head and tail sentinels) · every class has a stylesheet rule ·
all six JS functions present · no `<img>`/`data:`/`mask:`/font link/emoji · no unfilled
placeholders · `aria-expanded` matches `.open` everywhere · flowchart titles name events ·
at most 3 highlights · no unknown keys · balanced inline tags.

If it fails, fix the content. **Never write the HTML by hand instead** — a failed build means the
guide would have shipped wrong.
